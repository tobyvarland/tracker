<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <span class="navbar-brand mb-0 h1"><%= fa_icon "bullseye", class: "text-warning fa-2x" %></span>
  <button class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#tracker-nav">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="tracker-nav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= "active" if controller_name == "entries" && action_name == "index" %>">
        <a class="nav-link" href="/" tabindex="-1"><%= fa_icon "home", class: "fa-2x fa-fw" %></a>
      </li>
      <li class="nav-item <%= "active" if controller_name == "entries" && action_name == "charts" %>">
        <a class="nav-link" href="<%= charts_url %>" tabindex="-1"><%= fa_icon "line-chart", class: "fa-2x fa-fw" %></a>
      </li>
      <li class="nav-item <%= "active" if controller_name == "projections" && action_name == "index" %>">
        <a class="nav-link" href="<%= projections_url %>" tabindex="-1"><%= fa_icon "calendar", class: "fa-2x fa-fw" %></a>
      </li>
      <li class="nav-item <%= "active" if controller_name == "milestones" && action_name == "index" %>">
        <a class="nav-link" href="<%= milestones_url %>" tabindex="-1"><%= fa_icon "map-pin", class: "fa-2x fa-fw" %></a>
      </li>
    </ul>
  </div>
</nav>